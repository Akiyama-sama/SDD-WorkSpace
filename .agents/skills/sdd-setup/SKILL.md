---
name: sdd-setup
description: Use when the user wants to initialize or repair the repository SDD workspace environment, dependencies, or compatibility layer.
---

# sdd-setup

This skill is the Codex-compatible wrapper for the repository's `/sdd setup` workflow.

## Instructions

1. Read `.agents/commands/sdd/setup.md` in full before acting.
2. Use that file as the source of truth for environment preparation, dependency checks, user identity setup, and repo preparation.
3. If the user mentions `/sdd setup` or asks to initialize the SDD workspace, follow the setup command doc.
