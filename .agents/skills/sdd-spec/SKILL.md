---
name: sdd-spec
description: Use when the user wants to generate, resync, or amend specs, tasks, and execution translation artifacts in the repository SDD workflow.
---

# sdd-spec

This skill is the Codex-compatible wrapper for the repository's `/sdd spec` workflow.

## Instructions

1. Read `.agents/commands/sdd/spec.md` in full before acting.
2. Use that file as the source of truth for plan/full mode branching, create/resync/amend flows, and spec artifact generation.
3. If the user mentions `/sdd spec` or asks to generate/update specs, follow the spec command doc.
