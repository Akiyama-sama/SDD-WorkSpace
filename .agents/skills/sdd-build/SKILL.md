---
name: sdd-build
description: Use when the user wants to execute the implementation phase of the repository SDD workflow from plan-ready or spec artifacts.
---

# sdd-build

This skill is the Codex-compatible wrapper for the repository's `/sdd build` workflow.

## Instructions

1. Read `.agents/commands/sdd/build.md` in full before acting.
2. Use that file as the source of truth for dependency checks, plan generation, component chains, multi-track execution, and completion summaries.
3. If the user mentions `/sdd build` or asks to execute the planned work, follow the build command doc.
